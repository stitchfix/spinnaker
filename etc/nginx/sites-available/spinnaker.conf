server {
  listen         5000;
  location / {
    add_header Content-Type text/plain;
    return 200 'POOOOOOOOP';
  }
}

server {
  listen 80;
  return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    ssl_certificate /opt/spinnaker/ssl/cert.pem;
    ssl_certificate_key /opt/spinnaker/ssl/key.pem;

    location / {
        root /opt/deck/html;
    }

    location ~* ^/rosco/ {
        rewrite ^/rosco/(.*) /$1 break;
        proxy_pass http://localhost:8087;
    }
}
